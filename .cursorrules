## 中文响应规则
1. 无论用户使用何种语言提问，所有解释、建议和文档描述必须使用**简体中文**。
2. 保持技术术语的双语对应（例如：组件扫描 Component Scan）。

## Java 多模块开发规范
1. 当我询问依赖问题时，优先检查 Maven 的本地仓库缓存。
2. 在处理 `IllegalStateException` 时，优先考虑 Classpath 隔离和本地构建状态。
3. 代码示例必须符合 Java 17+ 标准，并使用中文注释。